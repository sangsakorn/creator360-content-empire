import React from 'react';
import ReactDOM from 'react-dom/client';
import Creator360ContentEmpire from './Creator360ContentEmpire';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <Creator360ContentEmpire />
  </React.StrictMode>
);
